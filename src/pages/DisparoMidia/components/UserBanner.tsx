// Este componente foi integrado diretamente no index.tsx
// Arquivo mantido apenas para referência histórica
// Pode ser removido quando conveniente

import { useCompanyStore } from '@/store/useCompanyStore';
import { useAuth } from '@/hooks/useAuth';
import { Building2, User, Send, Info } from 'lucide-react';

export function UserBanner() {
  return null; // Componente descontinuado
}
